import { Routes } from '@angular/router';
import { HeaderComponent } from './template/componentes/header/header.component';

const routes: Routes = [
    { path: 'header', component: HeaderComponent },
];

export const appRoutes: Routes = routes;
